#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - if else function
* Return: 0
*/
int main(void)
{
	int n;

	if (n > 0)
		printf("%d is posetive/n", n);
	else if (n < 0)
		printf("%d is negative/n", n);
	else if (n == 0)	
		printf("%d is Zero/n", n);
	return (0);

}
