#include "main.h"

/**
 * print_diagonal - check for a digit
 * @n : number of \\ to be printed
 * Return:void
 */

void print_diagonal(int n)
{
	int i = 0;

	while (i < n && n > 0)
	{
		i = 0;
		while (ii < i)
		{
			_putchar(' ');
			i++;
		}
		_putchar('\\');
		_putchar('\n');
		i++;
}
